# Simple Starknet RPC Provider test

This is a simple test of the Starknet RPC Provider using the Cartridge.gg API.

## Getting Started

1. Clone the repository
2. Run `pnpm install`
3. Run `pnpm run dev`
